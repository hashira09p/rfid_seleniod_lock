<!DOCTYPE html>
<html>
<head>
  <title>App</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <!-- Add Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
      /* Custom pink-red background for the header */
      .header-bg {
          background-color: #e6006d; /* pink-red background */
          width: 100vw; /* Make the header span the full width of the viewport */
          position: absolute; /* Ensure it spans the full width regardless of container */
          top: 0; /* Place it at the top of the page */
          left: 0; /* Align it to the left edge */
      }
      .container-custom {
          max-width: 100%; /* Remove container's default width constraint */
          padding-left: 0;
          padding-right: 0;
      }
      /* Style for the brand name */
      .brand-name {
          font-size: 1.5rem;
          font-weight: bold;
          color: white;
          margin-left: 10px;
      }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
<%#= render "admin/header" %>

<div class="container-fluid py-4">
  <div class="row">
    <!-- Main Content -->
    <main class="col-md-9 py-3">
      <%= yield %>
    </main>
  </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"></script>
<%= render "admin/footer" %>
</body>
</html>
