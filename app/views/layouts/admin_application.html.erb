<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>App</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <style>
      .header-bg {
          background-color: #e6006d;
          width: 100%;
      }

      .brand-name {
          font-size: 1.5rem;
          font-weight: bold;
          color: white;
      }

      .container-fluid {
          padding: 0;
      }

      .sidebar {
          height: 100vh;
          background-color: #f8f9fa;
          padding: 1rem;
          border-right: 1px solid #ddd;
      }

      main {
          background-color: #f8f9fa;
          border-radius: 5px;
      }

      footer {
          margin-top: auto;
      }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">

<%= render 'admin/header' %>

<div class="container-fluid">
  <div class="row">
    <!-- Side Menu -->
    <nav class="col-md-2 sidebar d-flex flex-column">
      <h5 class="mb-4">Menu</h5>
      <%= link_to "Professor", admin_root_path, class: "btn btn-outline-primary mb-2 #{'active' if current_page?(admin_root_path)}" %>
      <%= link_to "Cards", cards_path, class: "btn btn-outline-primary mb-2 #{'active' if current_page?(cards_path)}" %>
      <%= link_to "Room", rooms_path, class: "btn btn-outline-primary mb-2 #{'active' if current_page?(rooms_path)}" %>
      <%= link_to "Schedules", schedules_path, class: "btn btn-outline-primary mb-2 #{'active' if current_page?(schedules_path)}" %>
    </nav>


    <!-- Main Content Section -->
    <main class="col-md-10 py-4">
      <%= yield %>
    </main>
  </div>
</div>

<%= render 'admin/footer' %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
